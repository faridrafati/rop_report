// plotly.js-dist-min ships a prebuilt bundle without type declarations; we only
// use it through react-plotly.js/factory, so an opaque module declaration suffices.
declare module 'plotly.js-dist-min';
